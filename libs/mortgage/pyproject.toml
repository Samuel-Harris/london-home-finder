[project]
name = "lhf-mortgage"
version = "0.1.0"
description = "First-time-buyer mortgage viability domain for London Home Finder"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling==1.31.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lhf"]
